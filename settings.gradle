pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'gdx-webrtc'
include 'core', 'common', 'teavm', 'server', 'android', 'ios'
